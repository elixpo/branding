# docs — guides

How-to documentation for working with the asset pipeline.

| File | What it covers |
|---|---|
| [`pollinations-api.md`](pollinations-api.md) | The Pollinations generation API — text, image, video, audio, embeddings, auth, CLI and MCP. The endpoint behind every generator in [`../pipeline/`](../pipeline/). |

For brand **reference** (mascot rules, palette, model registry), see
[`../references/`](../references/).
